General:
	enter a string and it gets reversed
	
	Post and inspect the magic number

	add all required values and send the request
	
	base64 encoded username and password

	IBM websphere user xor encoding with "_" i.e ascii 95 as the key to encode

	find the respective plain text values

	openssl rsa -in priv -pubout // get public key from the private key

	openssl rsa -pubin -in pub -modulus -noout // get modules value
	openssl dgst -sha256 -sign priv mod | base64 // sign the value

	sudo docker exec -u 0 023b6e55f9cb cat /root/default_secret

		
